#pragma once
#include <WebServer.h>
#include <WebSocketsServer.h>
#include "ia_engine.h"

class ServidorAssincrono {
private:
    WebServer server;
    WebSocketsServer webSocket;
    uint8_t clientes;

    static const char INDEX_HTML[] PROGMEM;

public:
    ServidorAssincrono();
    void inicializar();
    void atualizar();
    void dispararStream(const TelemetriaIA& dados, uint32_t heap, uint32_t uptime);
    void gerenciarWS(uint8_t num, WStype_t type, uint8_t * payload, size_t length);
};
