#pragma once
#include <Arduino.h>

struct TelemetriaIA {
    char estado_atual[32];
    char previsao[32];
    float confianca;
    float precisao;
    float rmse;
    uint32_t total_ciclos;
    uint32_t total_anomalias;
};

class MotorIA {
private:
    TelemetriaIA dados;
    double welford_m;
    double welford_s;
    uint32_t contagem_welford;

public:
    MotorIA();
    void inicializar();
    bool atualizarMetricas(const String& pacote);
    void obterTelemetria(TelemetriaIA& dest);
    void configurarHistorico(uint32_t ciclos, uint32_t anomalias);
};
